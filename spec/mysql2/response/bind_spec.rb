# frozen_string_literal: true

RSpec.describe Mysql2::Response::Bind do
  it "has a version number" do
    expect(Mysql2::Response::Bind::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
